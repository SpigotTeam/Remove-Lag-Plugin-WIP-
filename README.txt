READ

This plugin is WIP. It routes connection between servers and users through the least congested 
networks (tested and approved). Also optimizes item packets and removes them if left for too long.
This WIP version is tested and it is optimized perfectly for any server purposes.